
const { httpSuccess,httpError} = require('../../utils/v1/httpResponse')

const createTask = async (req,res)=>{
    try {
        
    } catch (error) {
        console.log('error found in createTask',error)

    }
}

module.exports ={createTask}

